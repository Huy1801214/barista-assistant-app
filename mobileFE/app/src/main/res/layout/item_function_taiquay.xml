<?xml version="1.0" encoding="utf-8"?>
<!-- Sử dụng MaterialCardView để có thêm các thuộc tính shape và stroke -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardTaiQuay"
    android:layout_width="match_parent"
    android:layout_height="140dp"
    app:cardCornerRadius="@dimen/corner_radius_large"
    app:cardElevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/spacing_large">

        <ImageView
            android:layout_width="@dimen/icon_size_large"
            android:layout_height="@dimen/icon_size_large"
            android:src="@drawable/taiquay"
            android:contentDescription="Tại quầy"
            app:tint="@color/icon_green" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tại quầy"
            android:textColor="@color/icon_green"
            android:textStyle="bold"
            android:textSize="@dimen/font_size_large"
            android:layout_marginTop="@dimen/spacing_medium"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>